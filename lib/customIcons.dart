import 'package:flutter/material.dart';

class CustomIcons {
  static const IconData menu = IconData(
      // menu为图标的名称
      0xe624, // 代表的是该图标的代码
      fontFamily: "CustomIcons" // 在pubspec.yaml中设置的字体名称，要对应上
      );
  static const IconData search = IconData(
      // menu为图标的名称
      0xece1, // 代表的是该图标的代码
      fontFamily: "CustomIcons" // 在pubspec.yaml中设置的字体名称，要对应上
      );
  static const IconData option = IconData(
      // option为图标的名称
      0xe6d5, // 代表的是该图标的代码
      fontFamily: "CustomIcons" // 在pubspec.yaml中设置的字体名称，要对应上
      );
}

// class CustomIcons {
//   static const IconData menu = IconData(0xe900, fontFamily: "CustomIcons");
//   static const IconData option = IconData(0xe902, fontFamily: "CustomIcons");
// }
